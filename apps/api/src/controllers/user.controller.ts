import { Request, Response } from "express";

export class UserController{
    createUserData(arg0: string, createUserData: any) {
      throw new Error("Method not implemented.");
    }
    async loginUser(req: Request, res: Response) {
        try {
            
        } catch (error) {
            
        }
    }
}